import 'package:flutter/material.dart'; import 'package:flutter_riverpod/flutter_riverpod.dart'; import '../../../app/app_routes.dart'; import '../../../app/providers.dart'; import '../../../core/widgets/app_async_state.dart';
class StudentHomeScreen extends ConsumerWidget {const StudentHomeScreen({super.key});@override Widget build(BuildContext context,WidgetRef ref){final user=ref.watch(currentUserProvider);return Scaffold(appBar:AppBar(title:const Text('مساحتك التعليمية'),actions:[IconButton(onPressed:()=>ref.read(authRepositoryProvider).signOut(),icon:const Icon(Icons.logout))]),body:user.when(loading:()=>const AppLoading(),error:(e,_)=>AppErrorState(message:'تعذر تحميل ملفك.',onRetry:()=>ref.invalidate(currentUserProvider)),data:(profile){if(profile==null)return const AppEmptyState(title:'لا يوجد ملف مستخدم',message:'سجل الدخول مجدداً للمتابعة.',icon:Icons.person_off);return ListView(padding:const EdgeInsets.all(20),children:[Text('مرحباً، ${profile.name}',style:Theme.of(context).textTheme.headlineSmall),const SizedBox(height:6),const Text('نحو قرار أكاديمي أوضح كل يوم.'),const SizedBox(height:20),Card(child:Padding(padding:const EdgeInsets.all(20),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('اكتمال الملف الشخصي',style:TextStyle(fontWeight:FontWeight.bold)),const SizedBox(height:12),const LinearProgressIndicator(value:.2),const SizedBox(height:8),const Text('أضف بياناتك الأكاديمية والمهارات والاهتمامات لتخصيص توصياتك.')]))),const SizedBox(height:20),GridView.count(shrinkWrap:true,physics:const NeverScrollableScrollPhysics(),crossAxisCount:2,childAspectRatio:1.4,mainAxisSpacing:12,crossAxisSpacing:12,children:[_Action('الاختبارات',Icons.quiz_outlined,AppRoutes.assessments),_Action('التوصيات',Icons.auto_awesome_outlined,AppRoutes.recommendations),_Action('دليل التخصصات',Icons.account_tree_outlined,AppRoutes.majors),_Action('استشاراتي',Icons.forum_outlined,AppRoutes.consultations)].map((item)=>Card(child:InkWell(borderRadius:BorderRadius.circular(20),onTap:()=>Navigator.pushNamed(context,item.route),child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Icon(item.icon),const Spacer(),Text(item.label,style:const TextStyle(fontWeight:FontWeight.w600))])))).toList())]);}));}}
class _Action{const _Action(this.label,this.icon,this.route);final String label,route;final IconData icon;}
